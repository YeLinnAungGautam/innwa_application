import 'package:flutter/material.dart';

class PaymentStatusCheckingScreen extends StatelessWidget {
  const PaymentStatusCheckingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Checking payment status...'),
      ),
    );
  }
}
