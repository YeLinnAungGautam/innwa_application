import 'package:flutter/material.dart';

class cart_image extends StatelessWidget {
  const cart_image({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}